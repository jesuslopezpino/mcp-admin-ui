export const environment = {
  production: false,
  baseUrl: 'http://localhost:8081',
  apiKey: 'dev-token',
  pollingIntervalMs: 1500,
  featureFlags: {
    rememberArgs: false  // Desactivar por ahora la feature inestable
  }
};